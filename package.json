{
  "name": "sign-language-buddy",
  "private": true,
  "version": "0.1.0",
  "description": "Real-time two-way captioning for video calls (see PLAN.md)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @slb/extension && npm run verify",
    "dev": "npm run dev -w @slb/extension",
    "verify": "node tools/verify-dist.mjs",
    "typecheck": "tsc -p packages/core --noEmit && tsc -p packages/extension --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build",
    "fetch-models": "node packages/extension/scripts/fetch-models.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
